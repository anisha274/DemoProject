# Traffic Pipeline

Traffic awareness using RGB videos.

## Setup
1. Use Python 3.12 or 3.11
2. `python -m venv .venv`
3. `pip install -e .`
4. Install correct torch wheel

## Usage
`python -m traffic_pipeline --help`
